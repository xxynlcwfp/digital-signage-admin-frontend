# Production: empty = same-origin relative paths (/api/admin/...).
# Nginx reverse-proxies /api to the backend. Do not hardcode server IP here.
#
# Cross-origin API only (optional):
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
