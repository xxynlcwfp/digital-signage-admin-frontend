# Axios: empty = same-origin /api/* (Vite dev server proxies to backend).
VITE_API_BASE_URL=

# Vite dev-server proxy target (vite.config.js only; not bundled for production).
VITE_DEV_PROXY_TARGET=http://localhost:8080
